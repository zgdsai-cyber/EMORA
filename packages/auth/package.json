{
  "name": "@emora/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Better Auth authentication and server-side authorization utilities.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@emora/database": "workspace:*",
    "@emora/emotional-core": "workspace:*",
    "better-auth": "^1.7.3",
    "drizzle-orm": "^0.45.2",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "postgres": "^3.4.9"
  }
}
