# Security

Server secrets belong in environment configuration and must never be exposed to browser bundles. Future phases must add tenant isolation, authentication, authorization, input validation, auditability, rate limiting, and secure secret handling before exposing product workflows.
